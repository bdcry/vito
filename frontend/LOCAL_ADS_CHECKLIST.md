# Чеклист на 25 марта — добивка edit + AI + финал

## Цель дня

Закрыть edit-flow и подключить AI-функции:

`/ads/:id` → `/ads/:id/edit` → AI-помощь → `save` → обратно на деталку.

## База, которая уже готова

- [x] Список `/ads` живой
- [x] Поиск, сортировка, фильтры и пагинация работают
- [x] `loading / error / empty` на списке есть
- [x] `react-router-dom` подключён
- [x] `GET /items` расширен полем `id`
- [x] Деталка `/ads/:id` уже живая
- [x] Кнопки перехода: назад к списку и на edit уже есть
- [x] Роут `/ads/:id/edit` существует

## Что обязательно должно быть готово сегодня

- [x] Каркас и верстка `/ads/:id/edit` собраны
- [x] Форма `/ads/:id/edit` заполняется начальными данными
- [x] Поля формы разложены по категориям (`auto` / `real_estate` / `electronics`)
- [x] Есть счётчик символов у описания
- [x] `Сохранить` вызывает `PUT /items/:id`
- [x] После успешного сохранения идёт редирект на `/ads/:id`
- [x] `Отменить` возвращает назад без сохранения
- [x] Черновик сохраняется в `localStorage`
- [x] После refresh можно восстановить draft
- [x] Работает кнопка «Улучшить описание»
- [x] AI-ответ можно вставить в форму
- [x] Работает кнопка «Определить рыночную стоимость»
- [x] Есть состояния загрузки и ошибки для AI-действий
- [x] Все страницы пройдены на UX-целостность
- [x] Ошибки API выглядят по-человечески
- [x] Skeleton / spinner добавлены там, где это реально видно

## Текущий фокус на сейчас

1. [x] Подсветка пустых необязательных полей после попытки сохранения
2. [x] При смене `category` сбрасывать старые `params`
3. [x] Draft в `localStorage`
4. [~] Только потом AI

## Что сделать первым

- [x] Собрать форму edit-страницы
- [x] Нормализовать данные из detail в форму
- [x] Сделать поля формы живыми и собрать значения в единый payload
- [x] Подключить `PUT /items/:id`
- [x] Подключить draft в `localStorage`
- [~] Только после этого подключать AI-кнопки

## Пошагово по edit без усложнений

### 1. Оживить форму

- [x] Выбрать один путь для формы и не прыгать между подходами
- [x] Не уходить сейчас в новый рефактор архитектуры
- [x] После загрузки `ad` заполнить форму начальными данными
- [x] Привязать все `input/select/textarea` к состоянию формы
- [x] Собрать значения формы в submit
- [x] При смене `category` сбрасывать старые `params`

### 2. Довести UI формы

 - [x] Добавить счётчик символов у описания
- [x] Показать обязательность `title` и `price`
- [x] Подсветить базовые ошибки без фанатизма
- [x] Подсветить пустые необязательные поля warning-цветом после submit

### 3. Сохранение

- [x] Собрать payload под backend-контракт
- [x] Проверить `price` перед отправкой
- [x] Подключить `useMutation`
- [x] Отправить `PUT /items/:id`
- [x] После успеха уйти на `/ads/:id`
- [x] `Отмена` должна возвращать без сохранения

### 4. Draft

 - [x] Сохранять данные формы в `localStorage`
 - [x] Привязать draft к `id` объявления
 - [x] При перезагрузке восстанавливать draft
 - [x] После успешного save удалять draft

### 5. AI

- [x] Сделать action для описания
- [x] Для AI-описания поддержать состояния кнопки:
  - `Придумать описание`, если поле пустое
  - `Улучшить описание`, если поле не пустое
  - `Выполняется запрос`, пока идёт генерация
  - `Повторить запрос`, после завершения запроса
- [x] Пока AI-запрос идёт, показывать loader рядом с иконкой и запрещать повторный запрос
- [x] После успешного запроса показывать tooltip/блок с ответом AI
- [x] В успешном AI-блоке должны быть кнопки `Применить` и `Закрыть`
- [x] `Применить` должен перезаписывать поле описания новым AI-текстом
- [x] После неуспешного AI-запроса показывать tooltip/блок ошибки с кнопкой `Закрыть`
- [x] Сделать `Применить` для описания
- [x] Сделать action для цены
- [x] Добавить loading/error для AI

## Приоритет без расползания

1. [x] Живая форма
2. [x] `PUT /items/:id`
3. [x] Подсветка пустых optional-полей
4. [x] Сброс `params` при смене `category`
5. [x] `draft` в `localStorage`
6. [~] AI-кнопки
7. [ ] README и финальная проверка

## Важный момент по данным

- [x] На чтении `price` может быть `number | null`
- [x] В форме нужно аккуратно нормализовать `price`
- [x] Перед `PUT` отправлять уже валидное число `>= 0`

## Минимум на вечер

- [x] Edit-flow живой: detail → edit → save → detail
- [~] AI улучшает описание и подставляет цену

## Если останется время

- [ ] Тёмная тема с сохранением в `localStorage`
- [ ] Или отмена запросов при переходах
- [ ] Или diff “было / стало”

## Техдолг и то, что делаем позже

- [ ] Декомпозировать текущий `ads-list-page` 24–25 марта
- [ ] Декомпозировать `ad-details-page`
- [ ] Вернуться к `layout switch`
- [ ] Если будет смысл, синхронизировать `q/page/category/needsRevision` с URL
